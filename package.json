{
  "name": "scissor_url_shortener",
  "version": "0.1.0",
  "private": true,
 
  "scripts": {
    "start": "node scissor_url_shortener_backend/index.js",
    "build": "cd scissor_url_shortener_backend && npm install && cd ../scissor_url_shortener_frontend && npm install && npm run build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
